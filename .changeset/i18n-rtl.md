---
"@lit-pigeon/editor": minor
---

Editor UI localization (i18n) and RTL support. All toolbar, palette, property-panel, preview, template, asset-manager, and rich-text strings are now translatable via `configureI18n`, and the editor sets its text direction automatically (`resolveDir`) so right-to-left locales render correctly.
