---
'@lit-pigeon/editor': minor
---

Add built-in UI catalogs for es, fr, de, pt-BR, it, nl, ja, zh-CN, ar and hi, each loaded on demand with `loadLocale()` and passed to `config.messages`. They are machine-translated and flagged for native review. `BUILT_IN_LOCALES` lists the codes.
